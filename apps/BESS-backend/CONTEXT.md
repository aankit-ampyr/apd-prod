# BESS-backend context

- **Role**: Backend for BESS platform users.
- **Databases**:
  - BESS platform DB for BESS-specific entities
  - users DB for common/shared user data
- **Rule of thumb**: platform data stays in BESS DB; user identity/profile data comes from users DB.

