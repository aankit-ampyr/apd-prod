{
  "name": "lazarus-restructured",
  "version": "1.0.0",
  "private": true,
  "description": "pnpm --filter ./packages/react-common build",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:bess": "rm -rf node_modules package-lock.json && npm install && cd apps/BESS-frontend && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "date-fns-tz": "^3.2.0",
    "html-to-image": "^1.11.13",
    "react-icons": "^5.6.0",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@types/qs": "^6.15.0"
  }
}
